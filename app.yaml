runtime: custom
env: flex
service: manageschoolnotesapi
runtime_config:
  python_version: 3.9

manual_scaling:
  instances: 1

resources:
  cpu: 1
  memory_gb: 0.5
  disk_size_gb: 10

env_variables:
  GOOGLE_APPLICATION_CREDENTIALS: C:/Users/samund/source/service_account_creds_manageschoolnotes.json

network:
  name: default
  subnetwork_name: default
